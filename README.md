# Custom server with TypeScript + Nodemon + GraphQL example

Install it and run the server:

```bash
npm install
npm run server
```

Run the client:

```bash
npm run dev
```

Run the client and the server:

```bash
npm run all
```
